<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    
    <style>
        body { 
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7f6; 
            min-height: 100vh;
        }

        .profile-header {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            padding: 50px;
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: 0 15px 40px rgba(37, 117, 252, 0.2);
            text-align: center;
        }

        .avatar-circle {
            width: 100px; height: 100px;
            background: rgba(255,255,255,0.2);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 3rem; margin: 0 auto 20px;
            border: 2px solid rgba(255,255,255,0.5);
        }

        .book-card {
            border: none; border-radius: 15px;
            transition: all 0.3s ease;
            background: white;
            box-shadow: 0 5px 15px rgba(0,0,0,0.03);
            overflow: hidden; height: 100%;
        }
        .book-card:hover { transform: translateY(-5px); box-shadow: 0 15px 30px rgba(0,0,0,0.1); }
        .book-img { height: 200px; object-fit: cover; }

        .navbar { background: linear-gradient(to right, #6a11cb, #2575fc) !important; }
    </style>
</head>
<body>

    <nav class="navbar navbar-dark mb-4 shadow-sm px-4">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/">📚 BookStore</a>
            <a href="/logout" class="btn btn-outline-light btn-sm rounded-pill px-3">Logout</a>
        </div>
    </nav>

    <div class="container">
        
        <div class="profile-header">
            <div class="avatar-circle">
                <i class="bi bi-person-fill"></i>
            </div>
            <h2 class="fw-bold"><%= user.name %></h2>
            <p class="opacity-75 mb-0"><i class="bi bi-envelope"></i> <%= user.email %></p>
            <div class="mt-3">
                <span class="badge bg-light text-primary rounded-pill px-3 py-2">
                    Total Uploads: <%= books.length %> Books
                </span>
            </div>
            <div class="mt-4">
                <a href="/" class="btn btn-outline-light rounded-pill px-4">← Back to Home</a>
            </div>
        </div>

        <h4 class="fw-bold text-dark mb-4 px-2 border-start border-4 border-primary ps-2">My Uploaded Books</h4>

        <div class="row g-4 mb-5">
            <% if(books.length > 0) { %>
                <% books.forEach(function(book) { %>
                    <div class="col-md-3 col-sm-6">
                        <div class="card book-card">
                            <% if(book.image) { %>
                                <img src="/uploads/<%= book.image %>" class="book-img card-img-top" alt="Book">
                            <% } else { %>
                                <div class="book-img d-flex align-items-center justify-content-center bg-light text-secondary">
                                    <i class="bi bi-book fs-1"></i>
                                </div>
                            <% } %>
                            
                            <div class="card-body">
                                <h6 class="card-title fw-bold mb-1 text-truncate"><%= book.name %></h6>
                                <p class="text-success fw-bold small mb-2">₹<%= book.price %></p>
                                
                                <div class="d-flex gap-2 mt-3">
                                    <a href="/edit/<%= book.id %>" class="btn btn-light btn-sm flex-grow-1 text-dark border">Edit</a>
                                    <form action="/delete/<%= book.id %>" method="POST" onsubmit="return confirm('Delete permanently?');" class="flex-grow-1">
                                        <button type="submit" class="btn btn-danger btn-sm w-100">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center py-5 bg-white rounded shadow-sm">
                    <h5>You haven't uploaded any books yet.</h5>
                    <a href="/add" class="btn btn-primary mt-2 rounded-pill">Sell Your First Book</a>
                </div>
            <% } %>
        </div>

    </div>

</body>
</html>